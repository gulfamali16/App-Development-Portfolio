name: todo_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Database
  sqflite: ^2.3.3
  path: ^1.9.0

  # Notifications - DOWNGRADED to stable version
  flutter_local_notifications: ^17.2.3

  # Permissions
  permission_handler: ^11.3.1

  # Background tasks
  workmanager: ^0.9.0+3

  # Timezone
  timezone: ^0.9.4

  # Utilities
  intl: ^0.19.0
  provider: ^6.1.2
  device_info_plus: ^10.1.2
  path_provider: ^2.1.4

  # Export features
  pdf: ^3.11.1
  share_plus: ^10.0.2

  # Google APIs (for future Drive backup)
  googleapis: ^13.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1  # ← FIXED: Just the package here

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png

# ← ADD THIS SECTION AT THE END:
flutter_launcher_icons:
  android: true
  image_path: "assets/logo.png"